{
  "name": "diffhawk",
  "version": "0.1.0",
  "description": "Local, agent-agnostic poller that auto-reviews GitHub PRs when you're the requested reviewer.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "diffhawk": "./bin/poll.mjs",
    "diffhawk-init": "./bin/init.mjs"
  },
  "scripts": {
    "poll": "node bin/poll.mjs",
    "init": "node bin/init.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@clack/prompts": "^1.7.0"
  }
}
