diff --git a/Sources/Alerter/AlerterCommand.swift b/Sources/Alerter/AlerterCommand.swift
index 11b4bdb..7372540 100644
--- a/Sources/Alerter/AlerterCommand.swift
+++ b/Sources/Alerter/AlerterCommand.swift
@@ -50,8 +50,8 @@ struct AlerterCommand: ParsableCommand {
     @Option(help: "Group ID for notification replacement.")
     var group: String?
 
-    @Option(help: "Bundle identifier of the app to impersonate. (default: com.apple.Terminal)")
-    var sender: String = "com.apple.Terminal"
+    @Option(help: "Bundle identifier used to attribute the notification.")
+    var sender: String = "io.github.suguspnk.openmergelens.notifier"
 
     @Option(help: "URL or path of an image to use as app icon.")
     var appIcon: String?
@@ -59,6 +59,9 @@ struct AlerterCommand: ParsableCommand {
     @Option(help: "URL or path of an image attached to the notification.")
     var contentImage: String?
 
+    @Option(help: "Local file URL to open when the notification is activated.")
+    var open: String?
+
     @Option(help: "Auto-close the notification after N seconds.")
     var timeout: Int = 0
 
@@ -142,6 +145,7 @@ struct AlerterCommand: ParsableCommand {
                 groupID: group,
                 appIcon: appIcon,
                 contentImage: contentImage,
+                open: open,
                 timeout: timeout,
                 outputJSON: json,
                 ignoreDnD: ignoreDnd,
diff --git a/Sources/Alerter/Info.plist b/Sources/Alerter/Info.plist
index fe8144d..e74146d 100644
--- a/Sources/Alerter/Info.plist
+++ b/Sources/Alerter/Info.plist
@@ -3,9 +3,23 @@
 <plist version="1.0">
 <dict>
 	<key>CFBundleIdentifier</key>
-	<string>fr.vjeantet.alerter</string>
+	<string>io.github.suguspnk.openmergelens.notifier</string>
+	<key>CFBundleName</key>
+	<string>OpenMergeLens</string>
+	<key>CFBundleDisplayName</key>
+	<string>OpenMergeLens</string>
+	<key>CFBundleExecutable</key>
+	<string>alerter</string>
+	<key>CFBundlePackageType</key>
+	<string>APPL</string>
+	<key>CFBundleShortVersionString</key>
+	<string>1.0</string>
+	<key>CFBundleVersion</key>
+	<string>3</string>
 	<key>CFBundleIconFile</key>
-	<string>AppIcon</string>
+	<string>OpenMergeLens</string>
+	<key>LSUIElement</key>
+	<true/>
 	<key>NSUserNotificationAlertStyle</key>
 	<string>alert</string>
 </dict>
diff --git a/Sources/Alerter/NotificationManager.swift b/Sources/Alerter/NotificationManager.swift
index 8a616e1..cb178e9 100644
--- a/Sources/Alerter/NotificationManager.swift
+++ b/Sources/Alerter/NotificationManager.swift
@@ -1,4 +1,5 @@
 import AppKit
+import UserNotifications
 
 struct NotificationConfig {
     let title: String
@@ -12,14 +13,16 @@ struct NotificationConfig {
     let groupID: String?
     let appIcon: String?
     let contentImage: String?
+    let open: String?
     let timeout: Int
     let outputJSON: Bool
     let ignoreDnD: Bool
     let uuid: String
 }
 
-class NotificationManager: NSObject, NSUserNotificationCenterDelegate {
+class NotificationManager: NSObject, NSUserNotificationCenterDelegate, UNUserNotificationCenterDelegate {
     static let shared = NotificationManager()
+    private static let openReportAction = "OPEN_REPORT"
 
     private var currentNotification: NSUserNotification?
     private var currentConfig: NotificationConfig?
@@ -30,6 +33,13 @@ class NotificationManager: NSObject, NSUserNotificationCenterDelegate {
     func deliverNotification(config: NotificationConfig) {
         currentConfig = config
 
+        if config.timeout == 0 {
+            DispatchQueue.main.async { [weak self] in
+                self?.deliverPersistentNotification(config: config)
+            }
+            return
+        }
+
         // Remove earlier notification with the same group ID
         if let groupID = config.groupID {
             removeNotification(groupID: groupID)
@@ -99,6 +109,96 @@ class NotificationManager: NSObject, NSUserNotificationCenterDelegate {
         center.deliver(notification)
     }
 
+    private func deliverPersistentNotification(config: NotificationConfig) {
+        let center = UNUserNotificationCenter.current()
+        center.delegate = self
+        center.requestAuthorization(options: [.alert, .sound]) { granted, error in
+            if let error = error {
+                fputs("alerter: \(error.localizedDescription)\n", stderr)
+                exit(EXIT_FAILURE)
+            }
+            guard granted else {
+                fputs("alerter: notification permission was denied\n", stderr)
+                exit(EXIT_FAILURE)
+            }
+
+            let content = UNMutableNotificationContent()
+            content.title = config.title
+            content.subtitle = config.subtitle ?? ""
+            content.body = config.message
+            if let sound = config.sound {
+                content.sound = sound == "default"
+                    ? .default
+                    : UNNotificationSound(
+                        named: UNNotificationSoundName(rawValue: sound)
+                    )
+            }
+            if config.ignoreDnD {
+                content.interruptionLevel = .timeSensitive
+            }
+            if let open = config.open {
+                content.userInfo["open"] = open
+                content.categoryIdentifier = "OPENMERGELENS_REPORT"
+                let viewAction = UNNotificationAction(
+                    identifier: Self.openReportAction,
+                    title: "View results",
+                    options: [.foreground]
+                )
+                let category = UNNotificationCategory(
+                    identifier: content.categoryIdentifier,
+                    actions: [viewAction],
+                    intentIdentifiers: [],
+                    options: []
+                )
+                center.setNotificationCategories([category])
+            }
+
+            let identifier = config.groupID ?? config.uuid
+            center.removeDeliveredNotifications(withIdentifiers: [identifier])
+            let request = UNNotificationRequest(
+                identifier: identifier,
+                content: content,
+                trigger: nil
+            )
+            center.add(request) { error in
+                if let error = error {
+                    fputs("alerter: \(error.localizedDescription)\n", stderr)
+                    exit(EXIT_FAILURE)
+                }
+                print("@DELIVERED", terminator: "")
+                exit(EXIT_SUCCESS)
+            }
+        }
+    }
+
+    func listenForPersistentActivation() {
+        UNUserNotificationCenter.current().delegate = self
+        NSApplication.shared.setActivationPolicy(.accessory)
+    }
+
+    func userNotificationCenter(
+        _ center: UNUserNotificationCenter,
+        didReceive response: UNNotificationResponse,
+        withCompletionHandler completionHandler: @escaping () -> Void
+    ) {
+        defer {
+            completionHandler()
+            DispatchQueue.main.async {
+                NSApplication.shared.terminate(nil)
+            }
+        }
+        guard
+            response.actionIdentifier == UNNotificationDefaultActionIdentifier ||
+                response.actionIdentifier == Self.openReportAction,
+            let value = response.notification.request.content.userInfo["open"] as? String,
+            let url = URL(string: value),
+            url.isFileURL
+        else {
+            return
+        }
+        NSWorkspace.shared.open(url)
+    }
+
     // MARK: - Remove
 
     func removeNotification(groupID: String) {
diff --git a/Sources/Alerter/main.swift b/Sources/Alerter/main.swift
index aaf379b..79ad8e2 100644
--- a/Sources/Alerter/main.swift
+++ b/Sources/Alerter/main.swift
@@ -12,4 +12,11 @@ signal(SIGINT) { _ in
     exit(EXIT_FAILURE)
 }
 
-AlerterCommand.main()
+let launchArguments = CommandLine.arguments.dropFirst()
+if launchArguments.isEmpty ||
+    launchArguments.allSatisfy({ $0.hasPrefix("-psn_") }) {
+    notificationManager.listenForPersistentActivation()
+    NSApplication.shared.run()
+} else {
+    AlerterCommand.main()
+}
